import 'package:flutter/material.dart';

class KEventContentTabView extends StatelessWidget {
  const KEventContentTabView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Events content'),
    );
  }
}